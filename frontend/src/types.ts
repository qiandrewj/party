export interface Recipe {
  name: string;
  description: string;
  minutes: number;
  ingredients: string;
  link: string;
}

export interface Playlist {
  name: string;
  songs: string;
  artist: string;
}
